<%= render partial: 'shared/profile', locals { user: @user, profile: @user.profile } %>

<%= render partial: 'shared/users_list', locals { users: @user } %>

<%= render partial: 'shared/posts', locals { posts: @user.posts } %>

<%= link_to 'Home', root_path %>
